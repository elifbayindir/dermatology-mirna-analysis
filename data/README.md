# Synthetic data

`synthetic_mirna_cohort.csv` is generated inside the notebook with NumPy random distributions and seed `20260915`.

The file contains no real patient information. Its sample size, values, group effects, missingness, and quality flags are artificial. They were chosen to make the statistical workflow visible.

Do not use this dataset for clinical inference, prevalence estimates, diagnostic development, or biological claims.

